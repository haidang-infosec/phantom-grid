apiVersion: v1
kind: Service
metadata:
  name: phantom-grid-fleet
  namespace: kube-system
  labels:
    app: phantom-grid
    component: fleet-manager
spec:
  type: ClusterIP
  ports:
    - port: 9999
      targetPort: 9999
      protocol: TCP
      name: web
  selector:
    app: phantom-grid
    component: fleet-manager
